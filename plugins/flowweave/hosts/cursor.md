# FlowWeave for Cursor

Cursor is treated as a manual Agent Inbox host.

Open the project, read `.flowweave/agent-context.md`, read the exact run-specific `.flowweave/runs/<run-id>/agent-request.json` path provided by FlowWeave, and write one Agent Inbox v2 `agent-response.json` to the exact `responsePath` in that request.
