# FlowWeave for Cursor

Cursor is treated as a manual desktop bridge host.

Open the project, read `.flowweave/agent-context.md`, process pending requests under `.flowweave/agent-bridge`, and write `response.json` to the request's `responsePath`.
