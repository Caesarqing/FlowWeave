# FlowWeave for Claude

Use the FlowWeave Agent Inbox v2 instructions when working in Claude Desktop, Claude Code, or Claude Code CLI.

Read the run-specific `.flowweave/runs/<run-id>/agent-request.json` path provided by FlowWeave and write exactly one `agent-response.json` to the request's `responsePath`. Do not return the final artifact only in chat or stdout.
