# FlowWeave for Gemini

Gemini hosts should read `.flowweave/agent-context.md` and process `.flowweave/agent-bridge/pending-requests.json` when asked to use FlowWeave context.

Write Agent Protocol v1 `response.json` to the exact `responsePath` from the request.
